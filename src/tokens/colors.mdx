import { ColorPalette, ColorItem, Title, Meta } from "@storybook/blocks";
import { black, white, colors, currentColor, transparent } from "./colors";

<Meta title="Theme/Colors" />
<Title>Colors Palette</Title>

<ColorPalette>
    {Object.entries(colors)
        .filter(([, value]) => typeof value !== "string")
        .map(([name, value]) => (
            <ColorItem key={name} title={name} colors={value} />
        ))}
</ColorPalette>
