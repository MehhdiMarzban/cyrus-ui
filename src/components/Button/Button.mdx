import {
    Meta,
    Title,
    Subtitle,
    Subheading,
    Primary,
    Controls,
    Story,
    Stories,
    Canvas,
    Markdown,
} from "@storybook/blocks";
import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} />

## <Title>Button</Title>

<Subtitle> this is a simple component to show a button with a beautiful style . </Subtitle>

a button is thing that you press in under to make stuff happen.

<Subheading>Usage :</Subheading>

```javascript
import { Button } from "cyrus-ui";

    const App = () => {
        return <Button variant="primary" size="large">cyrus-ui</Button>;
    }

export default App;
```

<Primary />

## Controlls

buttons have lots of little things that you can tweak about them.

<Controls />

{/* canvas is for show in rectangle and can see code , and is like exacly in autodocs */}
{/* <Canvas of={ButtonStories.secondary}/> */}

{/* is like simple component is */}
{/* <Story of={ButtonStories.secondary}/> */}

<Stories meta={ButtonStories} title="All models" />
